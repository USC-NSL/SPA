/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf32-nacl)
GROUP ( libc.so.a0e12298 libc_nonshared.a  AS_NEEDED ( ld-nacl-x86-32.so.1 ) )
